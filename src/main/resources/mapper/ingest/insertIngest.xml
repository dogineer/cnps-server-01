<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.develop.web.domain.media.ingest.mapper.IngestMapper">

    <insert id="insertIngestRequest" parameterType="IngestRequestData">
        insert
        into ingest(f_member_id, title, program, name, phone, codec)
        values (#{memberId}, #{title}, #{program}, #{name}, #{phone}, #{codec})
    </insert>

    <insert id="insertMetadata" parameterType="Metadata">
        insert
        into metadata(id,
                      p_id,
                      file_name,
                      ext,
                      width,
                      height,
                      format_name,
                      format_long_name,
                      tags,
                      duration,
                      size)
        values (#{id}, #{filename}, #{ext},#{width}, #{height}, #{format_name}, #{format_long_name}, #{tags}, #{duration}, #{size})
    </insert>
</mapper>