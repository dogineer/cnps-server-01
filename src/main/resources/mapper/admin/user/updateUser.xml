<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.develop.web.domain.admin.user.mapper.AdminMapper">
    <update id="updateUserInfo" parameterType="UpdateUserInfoDto">
        UPDATE emp
        SET account = #{account},
            name    = #{name},
            phone   = #{phone},
            rank_id = #{rankId},
            dept_id = #{deptId},
            team_id = #{teamId},
            gender  = #{gender},
            email   = #{email}
        WHERE account = #{account};
    </update>

    <update id="updateAccess" parameterType="String">
        UPDATE emp
        SET access      = 1,
            approved_at = (CURRENT_TIMESTAMP)
        WHERE account = #{account}
    </update>

    <update id="updateUserDeleteFlag" parameterType="String">
        UPDATE emp
        SET del_fl = 1
        WHERE account = #{account}
    </update>
</mapper>