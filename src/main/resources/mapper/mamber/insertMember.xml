<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.develop.web.domain.auth.mapper.AuthMapper">
    <insert id="sign_up" parameterType="Member">
        INSERT
        INTO emp(account, name, phone, email, dep, role, des, password)
        VALUES (#{account}, #{name}, #{phone}, #{email}, #{dep}, #{role}, #{des}, #{password})
    </insert>
</mapper>