<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.develop.web.domain.service.clip.mapper.ClipMapper">
    <select id="insertClipData" resultType="ResultRequestData">
        insert
        into clip(ingest_id, program_id, folder_id, archive_metadata_id)
        values (#{ingest_id}, #{program_id}, #{folder_id}, #{archive_metadata_id})
    </select>
</mapper>